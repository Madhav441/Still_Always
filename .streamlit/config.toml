# Streamlit theme - tuned to match the custom CSS in app.py.
[theme]
base = "dark"
primaryColor = "#e7a8d6"
backgroundColor = "#15101c"
secondaryBackgroundColor = "#1d1530"
textColor = "#f6efe9"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
